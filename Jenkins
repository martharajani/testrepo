pipeline {
  agent any
  stages {
    stage('Submit Stack'){
      steps{
        sh "aws cloudformation create-stack --stack-name CalSam --template-body file://CalcSAM.yaml --tags Key=Name,Value=JenkinsCICD"
      }
    }
  }
  
}
