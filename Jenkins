pipeline {
  agent any
  stages {
    stage('Submit Stack'){
      steps{
        sh "aws cloudformation create-stack --stack-name jenkinsCF --template-body file://rajani.yaml --tags Key=Name,Value=JenkinsCICD --region ap-south-1"
      }
    }
  }
  
}
