pipeline {
  agent any
  stages {
    stage('Submit Stack'){
      steps{
        sh "sam deploy --template-file package.yml --stack-name rajaniCalc --capabilities  CAPABILITY_IAM CAPABILITY_AUTO_EXPAND"
        #sh "aws cloudformation create-stack --stack-name SamTemplate --template-body file://rajani.yaml"
      }
    }
  }
  
}
